@extends('layouts.app')
@section('title','Edit contact')
@section('content')
<div class="entry-page-header d-flex justify-content-between align-items-center mb-3"><div><span class="section-label">{{ ucfirst($kind) }} center</span><h1 class="h5 mb-0 mt-1">Edit {{ $contact->name }}</h1></div><a class="btn btn-sm btn-light" href="{{ route($kind==='vendor'?'vendors.show':'customers.show',$contact) }}"><i class="fa-solid fa-arrow-left" aria-hidden="true"></i> Back to profile</a></div>
<form method="post" action="{{ route($kind==='vendor'?'vendors.update':'customers.update',$contact) }}">@csrf @method('PUT')<div class="card"><div class="card-body p-3 p-lg-4"><x-contact-fields :contact="$contact"/><div class="mt-3"><div class="form-check"><input type="hidden" name="active" value="0"><input class="form-check-input" type="checkbox" id="contact-active" name="active" value="1" @checked(old('active',$contact->active))><label class="form-check-label" for="contact-active">Active {{ $kind }}</label></div></div></div><div class="card-footer bg-white d-flex justify-content-end gap-2"><a class="btn btn-light" href="{{ route($kind==='vendor'?'vendors.show':'customers.show',$contact) }}">Cancel</a><button class="btn btn-primary"><i class="fa-solid fa-floppy-disk" aria-hidden="true"></i> Save changes</button></div></div></form>
@endsection
