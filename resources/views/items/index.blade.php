@extends('layouts.app')
@section('title','Items')
@section('content')
<x-page-header eyebrow="Inventory" title="Items" description="Maintain products, units, prices, and reorder controls."/>
<div class="card"><x-toolbar placeholder="Search SKU, item, or barcode..." :filters="['state' => ['label' => 'Stock state', 'options' => ['all' => 'All stock states', 'active' => 'Active items', 'low' => 'Low stock', 'inactive' => 'Archived items']]]" import-url="{{ route('items.import') }}" create-label="Add item" create-url="{{ route('items.create') }}"/>
<div class="table-responsive"><table class="table table-hover"><thead><tr><th>SKU</th><th>Barcode</th><th>Item name</th><th>Item group</th><th>Unit</th><th class="text-end">Cost</th><th class="text-end">Base price</th><th class="text-end">On hand</th><th>State</th><th></th></tr></thead><tbody>@forelse($items as $row)<tr><td class="fw-semibold">{{ $row->sku }}</td><td>{{ $row->barcode ?: '-' }}</td><td>{{ $row->name }}</td><td>{{ $row->group?->name ?? 'Uncategorized' }}</td><td>{{ $row->unit }}</td><td class="text-end">${{ number_format($row->cost,2) }}</td><td class="text-end">${{ number_format($row->sale_price,2) }}</td><td class="text-end">{{ $row->quantity }}</td><td><x-status :value="!$row->active?'inactive':($row->quantity<=$row->reorder_level?'low':'healthy')"/></td><td class="text-end">@if(auth()->user()->role !== 'viewer')<a class="table-action" href="{{ route('items.edit',$row) }}" title="Edit {{ $row->name }}"><i class="fa-solid fa-pen" aria-hidden="true"></i><span class="visually-hidden">Edit</span></a>@endif</td></tr>@empty<tr><td colspan="10" class="text-center text-secondary py-5">No items found. Add your first inventory item to begin.</td></tr>@endforelse</tbody></table></div><div class="card-footer bg-white">{{ $items->links() }}</div></div>
<div class="modal fade" id="csvImportModal" tabindex="-1" aria-labelledby="csvImportTitle" aria-hidden="true"><div class="modal-dialog"><form class="modal-content" method="post" action="{{ route('items.import') }}" enctype="multipart/form-data">@csrf<div class="modal-header"><h2 class="modal-title fs-5" id="csvImportTitle">Import items from CSV</h2><button class="btn-close" type="button" data-bs-dismiss="modal" aria-label="Close"></button></div><div class="modal-body"><p class="small text-secondary">Required columns: <code>sku, name, unit, cost, sale_price</code>. Optional: <code>barcode, item_type</code>. Imports are validated before any item is created; opening stock is always zero.</p><label class="form-label" for="itemCsv">CSV file</label><input class="form-control" id="itemCsv" type="file" name="file" accept=".csv,text/csv" required></div><div class="modal-footer"><button class="btn btn-light btn-sm" type="button" data-bs-dismiss="modal">Cancel</button><button class="btn btn-primary btn-sm" type="submit"><i class="fa-solid fa-file-arrow-up" aria-hidden="true"></i> Import items</button></div></form></div></div>
@endsection
